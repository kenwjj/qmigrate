/ schemas/reference/instruments.q — splayed table (spec §8.2)

.qm.schema[`instruments] (
  .qm.splayed[];

  .qm.colx[`sym;    `symbol;  `attr`s];
  .qm.col [`name;   `symbol];
  .qm.col [`sector; `symbol];
  .qm.col [`active; `boolean]
  )
